import React, { useState, useEffect } from 'react';
import { View, Text } from 'react-native';

import styles from '../../utils/styles';

export default function Login() {
  return (
    <View style={styles.screen} >
      <Text style={styles.txtG}>Edit</Text>
    </View>
  );
}
